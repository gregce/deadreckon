# deadreckon Audit — 2026-05-11

**Subject:** how the alpha-tier as-built compares to the original 25 unmet needs.
**Audit date:** 2026-05-11
**Auditor:** deadreckon (rider P1)
**Source needs:** `/Users/gdc/stoa/docs/research/2026-05-10-unmet-needs/REPORT.md`
**As-built reference:** `/Users/gdc/deadreckon/docs/AS-BUILT-ARCHITECTURE.md`

## Needs Matrix

| # | Need (verbatim title) | Status | Evidence | Recommendation |
|---|---|---|---|---|
| 1 | Live Context And Spend Meter For Coding Agents | Resolved | `crates/deadreckon/src/tui_events.rs` streams same-process events and replays `events.jsonl`; `crates/deadreckon/src/main.rs` renders spend/context/wall-clock in the TUI. | Keep provider accounting tests as regressions. |
| 2 | Multi-Agent Worktree Coordination Layer | V1 | `docs/V1-CANDIDATES.md:3` records explicit sub-agent forking; `docs/AS-BUILT-ARCHITECTURE.md:1387` keeps scheduler or queue thin. | Keep lock and scope safety in alpha; queue and orchestration stay V1. |
| 3 | Infinite Undo For Agent Edits | Resolved | `docs/AS-BUILT-ARCHITECTURE.md:1372` lists `undo`; `crates/deadreckon-core/src/artifacts.rs:15` snapshots working trees. | Maintain snapshot tests as regressions. |
| 4 | Prompt-To-Code Provenance Audit Trail | Resolved | `docs/GAP-ANALYSIS.md:16` confirms trace/provenance id linkage; `crates/deadreckon-core/src/artifacts.rs:55` appends provenance. | Preserve append-only provenance across new refusal paths. |
| 5 | Searchable Team Memory For AI Coding Conversations | Resolved | `crates/deadreckon/src/main.rs` implements `library list|search|show`; `crates/deadreckon/tests/lifecycle.rs` covers scoped/all library queries, goal/date filters, and promoted-doc grep. | Keep promoted docs discoverable through library manifests. |
| 6 | Cross-Tool State Sharing Across Cursor, Claude Code, Codex, And Review Bots | Resolved | `crates/deadreckon/src/main.rs` normalizes JSONL/SQLite imports; `crates/deadreckon/tests/goldens/import/claude-code.show.golden` and sibling goldens cover normalized `show` output. | Review-bot import remains future source-specific work, not a harness gap. |
| 7 | Terminal-Agent Desktop UI With Diff, Files, And Session Dashboard | Partial | `crates/deadreckon/src/tui_events.rs` closes live event freshness and `crates/deadreckon/src/main.rs` renders files/docs/processes, but a full diff/desktop GUI remains absent. | Keep terminal TUI in alpha; defer desktop/diff surface to V1. |
| 8 | Agent Observability With Traces, Evals, And Failure RCA | Partial | `crates/deadreckon-core/src/artifacts.rs` records traces/provenance and `crates/deadreckon-core/src/gate.rs` records per-check acceptance proof; eval dashboards remain absent. | Keep local traces and gate proof; dashboard/RCA automation stays V1. |
| 9 | Disposable Sandboxes For YOLO Agent Work | Resolved | `crates/deadreckon-core/src/turn_loop.rs` writes and enforces `sandbox.toml`; `crates/deadreckon-sandbox/src/lib.rs` enforces per-tool filesystem/network policy. | Maintain adversarial sandbox tests. |
| 10 | Billing Guardrails For Agent Runs | Partial | `docs/AS-BUILT-ARCHITECTURE.md:1374` lists spend and wall caps; `docs/AS-BUILT-ARCHITECTURE.md:1382` keeps wall-clock budget richness thin. | Keep existing caps; richer subscription budget mapping remains V1. |
| 11 | Permission Boundaries For Unattended Agents | Resolved | `crates/deadreckon-core/src/turn_loop.rs` gates bash/write_file through `sandbox.toml` and records policy refusals with `try:` before mutation. | Add new tools only with explicit policy tests. |
| 12 | MCP Setup And Tooling Debugger | V1 | `docs/AS-BUILT-ARCHITECTURE.md:1395` lists MCP client surface as not built. | Keep out of Hardening v2; log deeper MCP UX separately if needed. |
| 13 | Team Onboarding From Real Agent Sessions | Resolved | `crates/deadreckon/src/main.rs` exposes docs through `doc`, TUI Markdown, and `library search`; `crates/deadreckon/tests/self_documenting.rs` covers generated run docs. | Keep run docs stable enough to train humans on prior sessions. |
| 14 | Structural Verification Before Agent Claims | Resolved | `crates/deadreckon-core/src/gate.rs` evaluates `acceptance.yaml` and signs per-check results; `crates/deadreckon-core/src/gate.rs` tests reject self-attestation. | Expand check kinds only with signed-proof tests. |
| 15 | Hooks And Test Gates Made Discoverable | Resolved | `crates/deadreckon/src/main.rs` groups help, enriches `status`, and makes `doctor` actionable; `crates/deadreckon/tests/lifecycle.rs` covers help/status/hint UX. | Hook system remains V1, but alpha discoverability is closed. |
| 16 | Provider Routing Without Losing Workflow | Resolved | `docs/AS-BUILT-ARCHITECTURE.md:1221` documents config; `crates/deadreckon-providers/src/lib.rs:438` builds fallback routes. | Keep model-selection UX covered by recent commits. |
| 17 | Shared Session Handoff Between Humans And Agents | Partial | `docs/AS-BUILT-ARCHITECTURE.md:1372` lists attach/show/import; `docs/V1-CANDIDATES.md:3` keeps fork handoff as V1. | Preserve local handoff verbs; multi-human cloud handoff remains V1. |
| 18 | Port And Environment Isolation For Parallel Agents | Partial | `docs/AS-BUILT-ARCHITECTURE.md:1360` lists sandbox dispatch; `docs/AS-BUILT-ARCHITECTURE.md:1387` says scheduler/queue is missing. | Keep worktree isolation; queue and port broker are V1. |
| 19 | Agent Security And Governance Receipts | Resolved | `crates/deadreckon-core/src/gate.rs` signs check-level proof results; `crates/deadreckon-core/src/turn_loop.rs` records `sandbox.toml` refusals in trace/provenance. | Preserve receipts across new tool types. |
| 20 | AI Review Limits And Paid Review Continuity | V1 | `docs/V1-CANDIDATES.md:3` captures forking; `docs/AS-BUILT-ARCHITECTURE.md:1397` excludes cost-aware routing. | Review-bot continuity and paid review workflow are out of this alpha pass. |
| 21 | Local-First Agent Data Control | Resolved | `docs/AS-BUILT-ARCHITECTURE.md:1221` shows local config; `docs/AS-BUILT-ARCHITECTURE.md:385` shows local run JSONL files. | Keep no-cloud default and read-only imports. |
| 22 | Meeting Action Items Connected To Code Work | V1 | `docs/AS-BUILT-ARCHITECTURE.md:1396` excludes cloud sync and voice capture. | Out of scope for deadreckon alpha CLI. |
| 23 | Prompt Libraries That Encode Team Standards | Partial | `docs/AS-BUILT-ARCHITECTURE.md:1546` documents skill resolution; `docs/AS-BUILT-ARCHITECTURE.md:1394` excludes hooks. | Keep skills; richer shared prompt library and hooks remain V1. |
| 24 | Execution-Efficiency Evals For Agent Runs | Partial | `crates/deadreckon-core/src/artifacts.rs` records traces/spend and `crates/deadreckon-core/src/gate.rs` records check results, but eval dashboards remain absent. | Local data is ready; dashboard/eval UI stays V1. |
| 25 | Agent Workspace Inventory And Run Queue | Partial | `crates/deadreckon/src/main.rs` implements scoped `list`, `status`, `cleanup`, and `library`; there is still no scheduler/queue. | Inventory is closed for alpha; queue orchestration remains V1. |

## Closures landing in this rider

- **P2 — TUI streaming.** Closes Need #1 freshness residue and Need #7 terminal UI latency by moving same-process attach to `RunEventBus` and cross-process attach to fast replay.
- **P3 — Cross-process kill.** Closes Need #11 and AS-BUILT §22.3 by writing `cancel.marker` and making the run loop honor it during long provider calls.
- **P4 — Mid-tool-call resume.** Closes AS-BUILT §22.2 by replaying truncated tool boundaries instead of skipping past them.
- **P5 — Sandbox per-tool policy.** Closes Need #9 and Need #11 for the alpha slice by adding durable `sandbox.toml` policy with `try:` refusals and provenance.
- **P6 — Acceptance YAML.** Closes Need #14 and strengthens Need #24 by recording structured per-check proof results.
- **P7 — Doctor exhaustive.** Closes the local discoverability slice of Need #15 by checking OS, permissions, versions, disk, and opt-in provider pings with `try:` hints.
- **P8 — Library query.** Closes the inventory/search slice of Need #5, Need #13, and Need #25 through `deadreckon library list|search|show`, date filters, and promoted-doc grep.
- **P9 — Import parity.** Closes Need #6 residue by golden-testing imported Claude Code, Codex, and Cursor sessions through the same `show` renderer.
- **P10 — Help/status polish.** Closes Need #15 usability residue and improves Need #25 status visibility with help groupings, next actions, library counts, and disk usage.

## P11 revisit

Hardening v2 changed the audit in these concrete ways:

- **Resolved:** Need #1, #5, #6, #9, #11, #13, #14, #15, and #19 moved from partial to resolved for the alpha scope.
- **Still partial by design:** Need #7, #8, #10, #17, #18, #23, #24, and #25 retain V1 residue such as diff/desktop UI, eval dashboards, richer subscription budgeting, multi-human handoff, hooks, and queueing.
- **Still V1:** Need #2, #12, #20, and #22 remain outside this CLI alpha.
- **Verification evidence:** P2-P10 depth tests live in `crates/deadreckon/src/tui_events.rs`, `crates/deadreckon-core/src/turn_loop.rs`, `crates/deadreckon-core/src/gate.rs`, `crates/deadreckon-sandbox/src/lib.rs`, `crates/deadreckon/tests/agentic_loop.rs`, and `crates/deadreckon/tests/lifecycle.rs`.
