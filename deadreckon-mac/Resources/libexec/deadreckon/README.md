Placeholder so the Resources/libexec folder reference exists before an
x86_64 vendor run: scripts/vendor-cli.sh places the x86_64 dr-gate here
(the arm64 dr-gate sits next to the CLI in Resources/bin).
