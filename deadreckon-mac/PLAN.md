# deadreckon-mac — build program

Executes `docs/design/MAC-APP-OPERATOR-CONSOLE.md` (§6.2 composite: Bridge shell, Quarterdeck home, Chartroom depth, converged Binnacle). Branch: `deadreckon-mac`. Phased commits, no pushes.

## Operator decisions (2026-08-06)

1. **App name:** deadreckon (the Mac app carries the product name).
2. **Repo home:** this repo, `deadreckon-mac/`.
3. **IA:** the §6.2 recommendation as written.
4. **Write parity:** the app (including the menubar popover) supports the same write verbs the CLI does — popover is not triage-only.
5. **Steering:** G6 plus widening — steer must work for any supported provider (between-turn steer-inbox consumption in the turn loop; codex-server keeps its mid-turn path).
6. **Notifications:** real user notifications, including the binary-side work (typed operator-attention events).
7. **Distribution:** straight to notarized releases, signed the same way deadreckon is (Developer ID Application: 4GRQMF5T5U; existing release-trust pipeline).
8. **Queue ranking:** durable facts only. No forward-looking estimates.

**Validation exemplar:** `/Users/gdc/getspecstory/specstory-mac` (note: `/users/gdc/getspecstoryai/mac-app` does not exist on this machine).

## Phases

| Phase | Scope | Status |
|---|---|---|
| R-M0 | G6 steerable-as-data · G8 live gate progress · G7 receipt audit · G10 diff patches · G3 fleet rollup · TAILING.md contract | **done** (trust review fixed 3 HIGH: marker-only attempt-scoped gate counts; fail-fast strict receipt path) |
| R-M1 | G1 JSON envelopes (9 verbs + global error envelope) · G2 start parity · G9 extend --note · steer widening (all providers) · notify events | in progress (G1 landed: `--json` on all nine verbs, global stdout error envelope with preserved exit codes; see §7 G1 "As built". G2 landed: `start --i-know-its-a-lot`, `--yes` alias on finish/apply/cleanup/undo, preview envelope embeds replayable `launch_plan`; see §7 G2 "As built". Steer widening landed: any provider steerable while Executing, between-turn inbox drain in the run loop with typed `steer_delivered` events, codex-server mid-turn path unchanged; see §7 G6 "As built (M1)". Notify events landed: typed `operator_attention` rows in run-local `notify.jsonl` (`docs/schemas/notify-event.schema.json`, checked + conformance-tested), reasons `verified_awaiting_promote` (emitted by the sealing path itself on a newly sealed two-key receipt) · `paused_at_cap` (run-loop process, always appended even with `[notify]` delivery disabled) · `blocked`/`failed`/`cancelled`/`waiting_input` (supervisor terminal classification; NeedsReview is the waiting-for-input state); rows are display-only, glossary-worded, carry real CLI `next_actions`, and existing delivery-attempt rows are unchanged — see docs/TAILING.md) |
| R-M2 | G4 finish --dry-run --json · finish --json | pending |
| R-M3 | G5 follow verb (merged NDJSON, replay offsets) | pending |
| APP-1 | Scaffold: project.yml, DeadreckonKit, CONTRACTS.md, vendor-cli.sh + manifest | **done** (BUILD SUCCEEDED; 35 Kit tests; exemplar-fidelity reviewed) |
| APP-2 | Menubar shell + Gate Queue home + Harbor + ⌘K | **done** (96 Kit tests; NEEDS REVIEW section + degraded menubar state added by review; Rust follow-up registered: glossary labels in list --json) |
| APP-3 | Chartroom workbench (narrative/spine/turns, evidence rail, drawer) | **done** + review fix pass (JobDetailStore + spine/turns/integrity derivations in the Kit, 167 Kit tests total; three-pane JobDetailView with live fleet sidebar, evidence rail, P5 drawer; steer/kill honest APP-4 placeholders; Open in Terminal live via apple-events with pasteboard degrade. Fix pass: verdict-on-JOB and show-diff-on-JOB never invoked/mislabeled (Rust gaps registered in CONTRACTS.md), reopen-safe close + window-lifecycle teardown, per-tail corruption surfaced, incremental O(new-rows) ticks, documented raw/supervisor ceilings, projection last-known-good, job-altitude spine NEXT) |
| APP-4 | Write parity: Lay Course, steer, kill, Binnacle promote, send-back, popover writes | **done** (MutationRunner single dispatcher: argv arrays through the CLIRunner seam, --json always, both envelope families decoded incl. the concatenated campaign-kill stream and the G1 carve-out; Lay Course sheet runs the G2 protocol exactly — preview, byte-honest plan-file replay, --i-know-its-a-lot armed ONLY by the typed-amount match; steer live everywhere with queued->delivered flipping only on the typed steer_delivered event; kill sheet states the real semantics, amber only from the envelope, terminal only from job-events.jsonl; Binnacle promote sheet with two-key band (report-recorded, verdict-JOB gap named), contract table, dry-run CANDIDATE decoding the G4 finish_plan shape spec-true with honest M2 degrade, destination flags 1:1, verbatim fail-closed refusals with zero override affordances; send-back note round trip; popover (.window style) carries quick-steer inline and Kill/Promote/Send back items that open the window onto the sheets. 224 Kit tests; done-contract authoring gap registered in CONTRACTS.md: start needs --acceptance or def-done --json for a GUI contract step. Review fix pass: `--` flag-termination before every operator-typed positional (dash-goals/notes are text, tested), in-flight double-submit guards on all five write coordinators (gated-CLI tests), kill tail primed past ledger history at dispatch (historical budget_exhausted can never resolve a kill; corrupt ledger → honest resolution-unavailable), finish_plan status/receipt decoded so a BLOCKED plan renders receipt.error verbatim (never "0 files"), M2-gap diagnosis reserved for the exit-2 clap carve-out, export --dest tilde-expanded with no invented default (promote disabled until a destination is typed), start --from project picker on both legs, ONE shared write client inside the quit-time SIGTERM sweep, may-have-queued disarm + explicit re-arm after envelope-free start/extend failures, steer delivered-flip order-independent, empty-output failures name the exit code, popover section precedence parenthesized, glossary words on kill terminal/probe chips, live-row promote gate + stale-preview notice + identity-checked contract table in the Binnacle) |
| APP-5 | User notifications (stable IDs, catch-up), popover, polish | **done** (AttentionCenter in the Kit tails notify.jsonl across the fleet — run roots resolved per the JobDetailStore convention with the verdict-on-JOB Single-shape fallback, bounded to 12 live tails via the pure AttentionTailPlan with a slow full-scan sweep for the rest; NotificationIntent identities derive from the record's own fields only (never observation time) with a UserDefaults bounded-LRU seen-set, so re-observation and relaunch cannot re-fire; launch catch-up fires only unseen identities within a 48 h recency window, deferred to the first non-empty fleet snapshot; per-reason + master prefs read live at post time; UNUserNotificationCenter behind the UserNotifying seam — adapter carries the exemplar's lazy-auth/stable-ID pattern with authorization resolved fresh per post (fix pass: no lifetime denial cache, a System Settings grant works without relaunch), categories Open + Review at Gate, responses routed through the Kit's pure router onto the job or the promote sheet. Fix pass also: off-main file I/O with a serialized async pollOnce, sticky per-(job,runID) corruption verdicts covering sweeps, issues[] rendered (Gate Queue header chip + Settings rows), approval-class catch-up waiver beyond 48 h while the decision still waits, true-LRU seen refresh, launch-at-login onChange re-entrancy guard. Settings scene (Cmd-comma): notification toggles, SMAppService launch-at-login with honest failure rendering, appearance-follows-system note (Theme needed nothing), read-only info rows (app/binary versions, vendored manifest sha per arch or the DEADRECKON_BIN override named, DEADRECKON_HOME provenance, schema-handshake gap stated with doctor as the health signal). Real menu bar via Commands: File New Job Cmd-N, View Gate Queue Cmd-1 / Search Cmd-K, Job Steer / Kill Cmd-Delete (routes to sheet) / Promote / Open in Terminal Cmd-T with enablement from the LIVE fleet row (same facts as the buttons), About panel with vendored CLI credits. Popover footer: supervisor line + Open/Start Job/Quit; spend-today dropped honestly (needs fleet-wide spend tails, documented). Real AppIcon set (flat anchor on deep blue) generated from scripts/app-icon.svg via rsvg-convert. 256 Kit tests) |
| VALIDATE | Build+tests green; UX/menu-flow benchmark vs specstory-mac; iterate until GREAT | **done — verdict GREAT** (round 1: four lenses GOOD, 14 fixes; round 2: two lenses GOOD, 8 fixes; round 3: GREAT, one copy nit fixed) |
| RELEASE | codesign (Developer ID 4GRQMF5T5U) + notarytool, riding the deadreckon release-trust pipeline | **built+signed+verified** (scripts/release-app.sh; CLI signed at vendor time so the sha pin covers signed bytes — BinaryLocator caught the sign-after-pin ordering live; notarization is one command with APPLE_ID/APPLE_TEAM_ID/APPLE_APP_PWD per docs/RELEASE.md) |

One commit per phase (more if a phase lands in coherent slices). Trust rules from the design doc hold throughout: the app never invokes dr-gate, never signs, no override affordances, gate-keys never read; dry-run promote is report-only.
